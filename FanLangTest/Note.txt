

******* 优化修改 *******    


语法、语义错误定位所在行数。    


优化：Turple/QueryStack/QueryLabel优化。    
优化：主编译单元和库的判断currUnit。  
优化：执行过程中的字符串连接操作。    


**** IL -> Native 参考  ****    

参考：给不同的东西赋值，LUA和C#都使用不同的中间代码指令：    

LUA中：    
变量赋值：
全局变量：SETGLOBAL
局部变量：SETLOCAL
对象成员赋值： SETTABLE
数组元素赋值：
单个索引：SETTABLE
多个索引：SETLIST

C#中：
给变量赋值使用 stloc 指令。
给对象成员赋值使用 stfld 指令。
给数组元素赋值使用 stelem 系列指令。  

REW：偏底层中间代码应该使用地址偏移（成员和元素都是）      


******* 当前工作 *******      

（DONE）外部函数调用。    

（DONE）强制类型转换。    

（DONE）调用C#类的静态函数。    

（DONE）拆箱装箱去除。    

（DONE）Compiler和上下文相关字段放在其他类例如ILUnit中。  

（DONE）库文件导入。    
	（格式暂定为.fanlib，预编译为中间代码）    
	（JUMP加入程序域字段或者基址偏移机制）              

（DONE）中括号表达式。（优先级和小括号一致，左结合）      

成员方法外部实现。    

